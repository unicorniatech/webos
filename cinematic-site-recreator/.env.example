# ============================================================
#  cinematic-site-recreator (frontend) — Environment Variables
#  Copia este archivo a .env.local y llena tus valores
#  NUNCA subas .env.local a git
# ============================================================

# URL del backend API (sin slash al final)
# En desarrollo local:
VITE_API_URL=http://localhost:3001

# En producción (cuando deploys el backend):
# VITE_API_URL=https://cinematic-api.tudominio.com
